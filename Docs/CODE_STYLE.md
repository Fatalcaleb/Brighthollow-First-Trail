# C# Code Style

- Use PascalCase for classes, public methods, and public properties.
- Use `_camelCase` for private fields.
- Prefer small methods with one clear responsibility.
- Keep runtime content data outside code when practical.
- Validate external data before use.
- Never commit experimental Academy work directly to `main`.
